# 📚 Contribution Guide 

このリポジトリは主に個人の学習記録 (TIL) として使用されますが、一貫性と品質を保つために、以下のGit運用ルールに従って更新してください。

## ⚙️ Git 運用ルール

### 1. Mainブランチの保護
`main` ブランチを直接編集（コミット、プッシュ）することは**厳しく禁止**します。

### 2. プルリクエスト（PR）によるマージ
すべての変更は、必ずフィーチャーブランチで行い、GitHub上でのプルリクエスト（PR）を経由してのみ `main` にマージされます。

### 3. ブランチ命名規則
変更を加えるためのブランチ名は、作業を行う日付を示す以下の形式で作成してください。

- **`YYYY-MM-DD`**
- **例:** `2025-11-19`

## 🚀 日々の更新手順

変更は必ずブランチを切り替えて行います。

### 1. 作業ブランチの作成

まず、`main` ブランチが最新であることを確認し、本日分の新しいブランチを作成・移動します。

```bash
# 1. mainブランチを最新にする
git switch main
git pull

# 2. 本日の日付のブランチを作成し、移動
git switch -c $(date +%Y-%m-%d)
```

### 2. コミットとプッシュ

学習記録を追加・編集し、コミット、プッシュします。

```bash
# 3. 変更をステージングし、コミット
git add .
git commit -m "変更差分に関するコメント"

# 4. リモートへプッシュ（-u は初回のみ）
git push -u origin $(date +%Y-%m-%d)
```

### 3. プルリクエスト (PR) の作成

GitHubウェブサイトで、プッシュしたブランチから `main` ブランチへのプルリクエストを作成し、マージします。

### 4. ローカルブランチのクリーンアップ

マージが完了したら、ローカルブランチを削除し、`main` を最新にします。

```bash
# mainブランチに戻る
git switch main

# マージ済みのブランチを削除（例: 2025-11-19）
git branch -d <ブランチ名>
```

---

## 🤖 カスタムコマンドを使った効率的なワークフロー

このリポジトリには、日々の運用を効率化するためのカスタムコマンドが用意されています。
Claude Code を使用している場合、以下のコマンドを活用することで、手作業を大幅に削減できます。

### 推奨ワークフロー

#### 1. ブランチ作成と学習記録の追加

```bash
# 本日の日付のブランチを作成
git switch -c $(date +%Y-%m-%d)

# Tech/ や Books/ に学習内容や読書メモを追加・編集
# （この時点ではまだ Diary/ ファイルは作成不要）

# 変更をコミット
git add .
git commit -m "feat: 学習内容を追加"
```

#### 2. 学習記録の自動生成（Claude Code）

Claude Code 上で以下のコマンドを実行：

```
/daily-summary
```

このコマンドは：
- git diff と git log を分析
- `Diary/YYYY/MM/YYYY-MM-DD.md` ファイルを自動生成
- 学んだことを分かりやすくまとめて記載

#### 3. 読書メモの著作権チェック（オプション）

Books/ を編集した場合は、コミット前に著作権リスクをチェック：

```
/check-book-notes
```

このコマンドは：
- Books/ 内の変更ファイルを自動検出
- 丸写しチェック、引用の適正化、画像の確認、独自性を審査
- 問題があれば具体的な修正ポイントを指摘

#### 4. プルリクエストの自動作成（Claude Code）

すべての変更が完了したら、以下のコマンドでPRを自動作成：

```
/create-pr
```

このコマンドは：
- Diary ファイルからトピックを自動抽出
- 簡潔なPRタイトルとボディを生成
- `gh pr create` で自動的にPRを作成

#### 5. PR マージとクリーンアップ

GitHubでPRをレビュー・マージした後：

```bash
git switch main
git pull
git branch -d <ブランチ名>
```

---

### カスタムコマンドの詳細

各カスタムコマンドの詳細な動作については、`CLAUDE.md` の「カスタムコマンド」セクションを参照してください。
